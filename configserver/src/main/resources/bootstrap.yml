#server.port=8888
#spring.cloud.config.server.git.uri=file://${user.home}/test/
#spring.cloud.config.server.git.username=hbanyal@connecture.com
#spring.cloud.config.server.git.password=hitendra77

server:
  port: 8888

eureka:
  instance:
    hostname: configserver
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kbastani/spring-boot-microservice-config